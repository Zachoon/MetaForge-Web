export function buildDecisionMoment(match){const decisions=match?.playerDecisions||[];if(!decisions.length)return null;const attack=[...decisions].reverse().find(x=>x.kind==="attack-window");if(attack)return {title:attack.tookPressureLine?"You chose pressure.":"You chose patience.",detail:attack.tookPressureLine?`Arena recorded ${attack.actors||"one or more"} attacker${attack.actors===1?"":"s"} on turn ${attack.turn}. This is evidence of that decision—not proof the line was correct.`:`Arena recorded an attack window with no attackers on turn ${attack.turn}. Forge needs board context before judging the choice.`,trait:"Pressure",source:"Explicit Arena action"};const block=[...decisions].reverse().find(x=>x.kind==="block-window");if(block)return {title:block.blocked?"You committed to the block.":"You preserved your board.",detail:`Arena recorded this combat decision on turn ${block.turn}. Match context is still required before comparing alternatives.`,trait:"Resource discipline",source:"Explicit Arena action"};const pass=[...decisions].reverse().find(x=>x.kind==="priority-pass");return pass?{title:"You passed the decision window.",detail:`Arena recorded a priority pass on turn ${pass.turn}. Forge will not call it hesitation without timing and available-action evidence.`,trait:"Decision tempo",source:"Explicit Arena action"}:null}
