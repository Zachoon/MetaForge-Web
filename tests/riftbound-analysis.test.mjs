import assert from"node:assert/strict";import test from"node:test";import{analyzeRiftboundStrategy,validateRiftboundMainDeck}from"../app/riftbound-analysis.mjs";
const catalog={cards:{alpha:{name:"Alpha"},beta:{name:"Beta"}}};
test("enforces official 40-card and three-copy main-deck rules",()=>{const result=validateRiftboundMainDeck([{name:"Alpha",quantity:4}],catalog);assert.equal(result.legal,false);assert.ok(result.issues.some(x=>x.type==="deck-size"));assert.ok(result.issues.some(x=>x.type==="copy-limit"))});
test("keeps full tournament legality explicitly bounded",()=>{const result=validateRiftboundMainDeck([{name:"Alpha",quantity:3},{name:"Beta",quantity:37}],catalog);assert.match(result.boundary,/Chosen Champion/) });
test("creates an offline Riftbound strategy read without inventing legality",()=>{const cards={runner:{name:"Runner",code:"R-1",text:{richText:{body:"<p>When I move, draw 1.</p>"}}},ganker:{name:"Ganker",code:"R-2",text:{richText:{body:"<p>[Ganking] When I attack, deal 2 to a unit.</p>"}}},answer:{name:"Answer",code:"R-3",text:{richText:{body:"<p>Stun a unit, then draw 1.</p>"}}}};const result=analyzeRiftboundStrategy([{name:"Runner",quantity:3},{name:"Ganker",quantity:3}],{cards});assert.equal(result.primary.key,"movement");assert.match(result.pressure.title,/Interaction/);assert.ok(result.theory.some(card=>card.name==="Answer"));assert.match(result.boundary,/eligibility must be verified/) });
