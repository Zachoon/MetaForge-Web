# Field Intelligence research store

Append-only JSONL observations for longitudinal Field Intelligence evidence.

- `observations.jsonl` — created by `npm run report:field-intelligence -- --live` or `--persist-research`
- No API secrets or player PII
- Deduplicated by stable fingerprints

Do not treat this store as Brain training input. Observation only.
