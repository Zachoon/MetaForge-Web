import assert from "node:assert/strict";import test from "node:test";import {buildDecisionMoment}from"../app/decision-moment.mjs";
test("turns an explicit attack into a bounded coaching moment",()=>{const moment=buildDecisionMoment({playerDecisions:[{kind:"attack-window",turn:3,tookPressureLine:true,actors:2}]});assert.equal(moment.title,"You chose pressure.");assert.match(moment.detail,/not proof/) });
test("does not invent a decision moment from a result",()=>assert.equal(buildDecisionMoment({result:"loss"}),null));
test("reviews captured alternatives using role and counterfactual regret",()=>{const moment=buildDecisionMoment({playerDecisions:[{kind:"attack-window",chosenId:"pass",state:{ownClock:3,opponentClock:8,boardAdvantage:.7,inevitability:-.4,uncertainty:.1},options:[{id:"attack",label:"Attack",pressure:1,tempo:1,terminalValue:.8,evidence:.9},{id:"pass",label:"Pass",flexibility:.1,evidence:.9}]}]});assert.equal(moment.strategicReview.status,"review");assert.match(moment.detail,/captured legal lines/) });
test("surfaces explicit state without pretending alternatives were known",()=>{const moment=buildDecisionMoment({playerDecisions:[{kind:"priority-pass",turn:5,observedState:{ownLife:8,opponentLife:11,ownHandSize:2,opponentHandSize:4,availableResources:3}}]});assert.match(moment.detail,/life 8–11/);assert.match(moment.detail,/hands 2–4/);assert.match(moment.detail,/available-action evidence/) });
