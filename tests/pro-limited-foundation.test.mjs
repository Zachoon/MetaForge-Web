import assert from "node:assert/strict"; import test from "node:test"; import { buildNewCardPrior, evidenceWeight } from "../app/pro-evidence.mjs"; import { evaluateDraftPairs, evaluateDraftPick, limitedDeckHealth } from "../app/limited-buddy.mjs";
test("expert perspective remains weaker than verified card facts",()=>{const now=new Date("2026-07-15"); assert.ok(evidenceWeight({kind:"card-fact",observedAt:"2026-07-14",provenance:{verified:true}},now)>evidenceWeight({kind:"expert-prior",observedAt:"2026-07-14",provenance:{verified:true}},now));});
test("new cards are hypotheses rather than fake results",()=>{assert.equal(buildNewCardPrior({name:"Marvel Card",tags:["tempo"]},[{kind:"expert-prior",tags:["tempo"],observedAt:"2026-07-14",provenance:{verified:true}}]).status,"untested-prior");});
test("draft ranking considers pool fit and health flags fundamentals",()=>{assert.equal(evaluateDraftPick([{name:"A",rating:2,colors:["G"],cmc:2},{name:"B",rating:2,colors:["U"],cmc:6}],[{colors:["G"]}],{pick:3})[0].name,"A"); assert.ok(limitedDeckHealth([]).warnings.length===2);});
test("pick-two ranks card combinations instead of two isolated winners",()=>{const pairs=evaluateDraftPairs([{name:"Green Two",rating:3,colors:["G"],cmc:2},{name:"Green Three",rating:2.9,colors:["G"],cmc:3},{name:"Blue Bomb",rating:3.05,colors:["U","B"],cmc:6}],[{colors:["G"]},{colors:["G"]}],{pick:3}); assert.deepEqual(pairs[0].cards.map(card=>card.name),["Green Two","Green Three"]);});
